# 项目日志_09_20260906_规模化诊断与结构优化

- **时间**：2026-09-06 15:28:58 CST（cmd 读取：`echo %date% %time%`）
- **任务**：全量实测项目状态，量化体量与完成度，输出结构与工作流的规模化优化建议
- **对象**：`第二本小说/` 全项目（非单章，无翻译产出）
- **产出报告**：`项目文档/规模化诊断与优化建议_20260906_152858.md`

---

## 一、执行内容

### 1.1 重跑进度基线（验证是否有进展）

`python tools/project_status.py` 全量重扫，全部统计项与 2026-09-02 基线**完全一致**：

| 指标 | 数值 |
|---|---|
| 章节目录数 | 90（第1~90章，无缺号） |
| 有初译_对照 | 84 |
| 有译文(纯英) | 78 |
| 有质量评估 | 79 |
| 有审校意见 | 31 |
| 有商业审校 | 5 |
| 三件套齐全 | 64 / 90 |

→ 结论：自 2026-09-02 起 **4 天零进展**，工程化改造停在任务 2b。

### 1.2 首次精确量化全书体量

| 指标 | 数值 | 方法 |
|---|---|---|
| 全本章数 | **351 章** | `归档/月上柳梢头，人约黄昏后.md` 正则匹配 351 个章节标题（第1章~第351章） |
| 全本字数 | 1,323,298 汉字 | 字符统计 |
| 单章中位 | 2,637 汉字 → **2,201 英文词** | 71 章原文 + 84 章对照实测 |
| 已产英文词 | **182,570** | 84 个对照版英文词合计 |
| 章节完成度 | **25.6%**（90/351） | — |
| 字数完成度 | **23.9%**（18.3万 / 76.3万） | 351 × 2,173 词投影 |
| **剩余** | **261 章 ≈ 58 万英文词** | 第91~351章 |

### 1.3 ✅ 澄清 Q7（351 章 vs 90 章矛盾）

此前判定"索引称 351 章、磁盘实测 90 章"为数据矛盾。**本轮实测：两者不矛盾**——
底稿确为 351 章；`project_status.py` 的 90 是**已建目录数**。
**真正结论：第91~351章连目录都未建立**，剩余工作量此前被严重低估（不是"补 26 章"，是"还有 261 章未开工"）。

### 1.4 新增风险点发现

| # | 风险 | 实测 |
|---|---|---|
| R1 | Git 未落盘 | 57 项变更（35 未跟踪 / 21 修改 / 1 删除）；最后提交停在 2026-08-02（`335175f`）；根目录存在 `.git.corrupt` 残留 |
| R2 | 三份副本并存 | 主 1286 文件/69.4MB · `副本` 3142 文件/44.9MB · `_recovery_clone` 797 文件/13.3MB |
| R3 | 命名 20 种变体 | 470 个章节 md 存在 20 种命名模式 |
| R4 | TM 句级记忆为零 | 已产 18.2 万英文词，`TranslationMemory/` 不存在，segments = 0 |

### 1.5 目录与命名实测

470 个章节 md 的命名模式分布（完整 20 种见报告 §5.1），主要问题：
- `第N章翻译质量评估报告.md`(73) vs `第N章翻译质量评估.md`(6) 同槽位异名
- `审校意见N-Gemini.md`(28) / `-Gemini .md`(2，含空格) / `_N-WorkBuddy.md`(3，多下划线) / `-Gemini copy.md`(1)
- `第N章译文.md`(77) vs `第N章纯译文.md`(1)，破坏"纯英版是派生物"约定
- `润色提升报告`(2) / `润色报告`(1) / `审校日志`(1) / `重复性与吸引力审计报告`(1) / `PhaseN审校整合报告`(1) 五类异名应归一

---

## 二、产出

- **`项目文档/规模化诊断与优化建议_20260906_152858.md`**（本报告的完整版）
  - 九节：体量量化 / 产物缺口 / 改造进度 / 风险点 / 目录结构优化 / 工作流杠杆 / 度量体系 / 执行顺序 / 待决问题
- 同步更新：`TODO.md`、`implementation.md`、`项目目录索引.md`

---

## 三、核心判断（写入长期记忆）

1. **当前瓶颈不是翻译能力，是「可验证性缺失」与「冷启动成本过高」**。26 章三件套缺口、12 章译文缺失、78 vs 84 不同步，都不是翻译问题，是一个 `sync_bilingual.py` 就能收敛的工程问题。
2. **唯一真正的阻塞点是人工裁定**：304 条 `[建议]` 术语堵住任务 3。解法不是"等 304 条裁完"，而是**按频次先裁前 50 条分批解锁**（覆盖约 80% 出现率）。
3. **TM 句级记忆是唯一"越做越快"的杠杆**，其余动作都只是"把欠账补平"。已产 18.2 万词却零 segments，越晚做欠账越大。
4. **批次大小（5 章/批）方案自身标注为待验证（C1）**，跑了 2 批实测速率前不应固化。

---

## 四、遗留与下一步

**未决问题更新**：Q7 ✅ 已澄清；新增 **Q9**（三份副本如何收敛）、**Q10**（`.git.corrupt` 处置）。
Q1–Q6、Q8 维持原状待用户裁定。

**建议下一步（P0，按序）**：
1. 提交 git 基线（57 项变更）+ 处置 `.git.corrupt` —— **所有结构调整的前置条件**
2. 裁定高频术语前 50 条 → `glossary.md` v1.0
3. 写 `tools/sync_bilingual.py`（含 `--init-md5`）→ 一次性收敛 26 章三件套缺口
4. 建 `_记忆索引.md` + `README.md` → 冷启动成本从"读 8 份日志"降到"读 1 页"

---

## 五、工程备注（可复用经验）

- **中文路径坑复现确认**：PowerShell 直传中文绝对路径会 GBK 乱码（`The string is missing the terminator`）。本轮绕过方法：用 `glob.glob('d:/MyData/projects/*/*/*/*/tools/project_status.py')` 等**全 ASCII 通配符**在 Python 内推导路径再执行，命令行全程不出现中文。此技巧可直接写入工具 README。
- **Unicode 转义技巧**：需要在 ASCII 命令行里匹配中文时，用 `\u7b2c`（第）`\u7ae0`（章）等转义写法构造正则，可完全规避编码问题。

---

# 补充记录 ①（19:xx）· 任务 3 起步：`tools/sync_bilingual.py` v1

- 依据：P0-2（规模化诊断报告 §八 建议执行顺序）。
- **对照版体制实测（84 章 3 种）**：A-sep（77 章，`---` 分隔）/ B-plain（4 章：第6/7/44/47 章）/ C-marked（3 章：第8/9/10 章，含 `**译文**:` 半角冒号变体）。
- **产出脚本**：`tools/sync_bilingual.py`（三格式解析 / 标题行过滤 / `>` 诗行续行合并 / `--init-md5` 原文 MD5 基线 / `--apply` 补缺 / `--force` 覆盖不同步 / `--report` 自动命名报告）。
- **全量 dry-run**：已同步 29 · 缺纯英版 7 · 不同步 45 · 无对照版 6。报告：`项目文档/双语同步校验报告_20260906_190948.md`。
- **事故与修正**：初版将纯英版 `---` 分隔行计为段落 → 误报 68 章不同步（实为解析 bug，修正后 45）；`--report` 中文路径参数触发 GBK 乱码、误建 `椤圭洰鏂囨。` 乱码目录（已清理）→ 改为无参自动命名。
- **未执行**：`--init-md5` 建基线、`--apply` 写文件均未跑（依赖 git 基线先行，P0-0 未完成前保持保守）。

# 补充记录 ②（19:xx）· 跨工作区 `.codebuddy/memory` 检索

- 检索范围：`d:/MyData/projects/.codebuddy`、`01-活跃项目/{.codebuddy,.workbuddy}`、`中英剧本MTPE/{.codebuddy,.workbuddy}`、`07_外贸业务/.codebuddy`、本项目 `.workbuddy`。
- **对本项目需要更新/补充的点**（详见项目长期记忆 MEMORY.md 与 TODO §二·〇）：
  1. **用户红线（2026-09-03，两处独立记忆重复记载）**：新建 rules / agents / skills / 工程性文件**只给框架 + `{{占位符}}`，禁止代填** → 任务 4/5 落地时全须骨架化，另建 `_占位符清单.md`。
  2. **时间取系统真实时间**（cmd/PowerShell 命令），不依赖注入的 current_time。
  3. **术语回溯缺陷（新实证）**：glossary 反建未扫项目日志与 Tab 表历史裁定 → **夏果 三重冲突**（Tab=`Xia Guo` / 日志05 L587=`Xia Gu` / glossary v0.9=`Xiaguo`）→ 列为 P0 待裁首例。
  4. 跨项目铁律 9 条入库（外部审校四象限 / 五类致命缺陷 / 蒸馏通用化 / 剥离 LOCAL 层 / git 大量 D 确认 / 编辑 ≤3 次 / 双 skill 前移 等）。
  5. 上层 `01-活跃项目` 记忆本项目路径已由 `02_网文创作/` 更正为 `01_翻译业务/`。
- **同步更新**：项目长期记忆 / TODO.md / implementation.md / 工作区记忆（01_翻译业务 与 01-活跃项目）。
