# Implementation — 第二本小说翻译工程化改造

> 记录**已实现内容、关键决策、接口变更、临时方案**。进度勾选见 `TODO.md`。
> 方案依据：`工程化改造方案_借鉴剧本MTPE.md`
> 最后更新：2026-09-02

---

## 一、关键决策（Decision Log）

| # | 决策 | 理由 | 状态 |
|---|---|---|---|
| D1 | 工程骨架照搬 MTPE，本地化策略反转重写 | 骨架解决的是"多会话多 AI 协作一致性"，与体裁无关；MTPE 的"完全西化"针对美国当代短剧，照搬会毁掉宋代古装的异域感 | 已定 |
| D2 | 对照版为唯一真源，纯英版为脚本派生物 | 杜绝"译文.md 缺失/未持久化/不同步"（第30章重建事故、第31-34章暂存删除） | 已定，待脚本落地 |
| D3 | 术语表一个词条只允许一个定译 | 现状 `Master / Shifu` 并列 = 把不一致写进权威源，即 MTPE 所称 AI 痕迹第 11 类"同义词循环" | 已定，待 glossary 落地 |
| D4 | knowledge 用**复制**回活跃路径，归档保留备份 | 规避 IDE 句柄导致移动失败；保留可回滚 | 执行中 |
| D5 | 门禁判据对齐现有规则而非加严 | G4 原设"单句≥40词=P0"会误伤无从句的文学性长句，改为"≥40词且连接词≥3=P0" | 已定 |
| D6 | humanize 暂按方案 A（合集级一份 + 引用） | 防双副本漂移；MTPE 四 IDE 同步已踩过同类坑 | 待验证 |
| **D7** | **进度基准切换**：以 `项目文档/项目进度基线.md`（`tools/project_status.py` 生成）为唯一基准；`项目目录索引.md` 降级为"结构与命名规范参考" | 实测 90 章 vs 索引记录 35 章，误差 55 章；按其决策会严重低估工作量 | 已定 |
| **D8** | 术语回溯范围以实测章节目录为准（81 章有术语文件），不采信索引的 35 章 | 同上 | 已定 |
| **D9** | **术语裁定改分批解锁**：304 条 `[建议]` 不一次性全裁，按频次先裁前 50 条（覆盖约 80% 出现率）即升 v1.0 并开锁任务 3，余 254 条滚动裁定 | 全量 304 条是纯人工吞吐瓶颈，AI 无权代裁（红线）→ 串行阻塞已致 4 天零进展 | 已定（2026-09-06） |
| **D10** | **任务 3 内部按 ROI 重排**：`sync_bilingual.py` 提为最优先（覆盖 G1/G2/G3/G4/G5/G12 中 6 项且直接收敛 26 章欠账），`check_ai_tells.py`（G6/G7）后置不阻塞 | 门禁不必一次上齐 12 项；先让"欠账可收敛、质量可验收" | 已定（2026-09-06） |
| **D11** | **全书体量基准固定**：351 章 / 1,323,298 汉字 / 单章中位 2,201 英文词 / 全书投影 762,881 英文词；批次按 5 章 ≈ 11,000 英文词换算，剩余 53 批 | 此前无体量基准，工作量只能按章数粗估 | 已定（2026-09-06） |
| **D12** | **Git 基线提交为所有结构调整的前置条件**（任务 7 提为 P0-0） | 实测 57 项未提交变更、最后提交停在 08-02，且存在 `.git.corrupt` 残留；无回滚点 | 已定（2026-09-06） |

## 二、接口变更（Interface Changes）

| 对象 | 变更前 | 变更后 | 影响面 |
|---|---|---|---|
| 术语权威源 | `术语表/术语表_Trados_Tab.txt`（扁平两列，人工维护到第35章） | `glossary.md`（分节 + 状态 + 变更记录），CSV/Tab 降级为导出物 | 全部 SOP / 工作流 / 脚本引用 |
| 术语导出工具 | `术语表/merge_glossary.py`（硬编码只扫第 1-5 章） | `tools/export_trados.py`（全表解析 + 状态列 + `--check` 反向校验） | 术语表文件 |
| 纯英版生成 | 手工从对照版提取，易缺失/不同步 | `tools/sync_bilingual.py` 生成 + 段落 1:1 校验 | 每章 `第X章译文.md` |
| RAG 检索路径 | `.agent/knowledge/...`（实际内容却在 `.agent/归档/knowledge/`） | **已修复**：复制回活跃路径（97 文件，含 `Knowledge_Index.md`、`translation_challenges.md` 两份此前未记录的索引） | `translation_sop.md` §1.1、`translate_chapter.md` Step 2.5 |
| 进度基准 | `项目目录索引.md`（记录 35 章，滞后） | `项目文档/项目进度基线.md`（实测 90 章，脚本可重生成） | 所有批次规划与工作量估算 |
| 术语回溯输入 | 人工翻章节 | `项目文档/术语候选与冲突清单.md`（753 唯一词 / 148 章内冲突 / 108 跨章冲突 / 28 复合 / 469 一致项） | `glossary.md` 重建 |

## 三、已实现内容

### 任务 0 · RAG 知识库检索路径修复（✅ 已完成）

- **问题**：`.agent/knowledge/` 活跃路径下无该目录，知识库整体位于 `.agent/归档/knowledge/`。SOP 与工作流的强制检索指令指向活跃路径 → 每批"术语注入"必然扑空。
- **处理**：`xcopy` 复制回活跃路径（**用复制不用 move**，规避 IDE 句柄；归档保留回滚备份）→ 校验 97 文件到位 → 复核 SOP §1.1 与工作流 Step 2.5 路径可执行。
- **附带发现**：库内还有 `Knowledge_Index.md`、`translation_challenges.md` 两份此前未记录的索引文件。
- **遗留**：characters 目录混入 3 个非角色切片（`3.3 笨拙的温柔.md`、`三、本章翻译注意事项.md`、`4. 剧情进展.md`），**未删**（删除需用户确认），已登记待清理。

### 任务 1 · 接线 humanize（✅ 已完成）

- `.agent/skills/translation_sop.md` 新增 **§三·五 去 AI 痕迹自检卡**：A 级零容忍（AI 高频词 / -ing 尾句浅析 / 系词回避 / 宣传腔 / 卷曲引号）、B 级每章计数上限（em dash ≤2、三段式 ≤1、否定排比 0）、C 级体裁专项（同义词循环 / `VERB+-ly` 弱填充副词 / 情绪加戏），明确优先序 **忠实原文 > 术语锁 > 去 AI 痕迹 > 表达自然**。
- `.agent/workflows/translation_optimization_workflow.md` 新增 **§1.5**（Phase 1 必过）+ 输出要求追加自检卡报告字段。

### 任务 2a · 术语候选与冲突扫描（✅ 已完成）

- 新增 `tools/build_glossary_candidates.py`（默认 dry-run，`--apply` 写报告）。
- 产出 `项目文档/术语候选与冲突清单.md`：扫描 90 个术语文件、1059 条目、753 唯一中文词条 → **章内并列冲突 148 / 跨章冲突 108 / 复合词条 28 / 可直接锁定 469**。
- **关键发现**：`A / B` 并列译法的源头在**各章术语文件本身**（如 `Prefectural Office / Government Office`），不止主术语表——故脚本把"英文含 `/`"判为章内冲突、"中文含 `/`"判为复合词条，分类处理。

### 任务 2b · glossary.md v0.9 生成（✅ 已完成）

- 复用同一脚本新增 `--emit-glossary`（**不重造轮子**，与冲突扫描共用解析器）。
- 产出 `glossary.md`：**767 词条 / 463 锁定 / 304 建议 / 42 条复合拆分并入 / 309 条待裁定清单**。
- 分类规则：角色名（RAG 角色库 41 切片名精确匹配）→ 称谓与官制 → 地理与机构（`府/院/殿/庄…` 后缀）→ 世界观与武功 → 衣食器物 → 诗词典故 → 金句口头禅 → 未分类。
- 惯例校验内置：`府→Manor`（提示）、`嬷嬷→Matron`（**自动从候选取姓拼 `Matron X` 并覆盖频次推荐**）、事故项强制锁定（仪王府 / 千年神柳 / 小爷）。
- 括号别名检测：`赵朴（仪王）` 自动标注"疑似别名，建议合并"。
- **生成后抽查发现并修复的缺陷**（MTPE「反向护栏」实践）：
  1. `邹嬷嬷` 按出现频次被推荐为 `Nurse Zou`（频次高于 `Nanny Zou`），但全书惯例是 `Matron Zou` → **说明"频次优先"不适用于有惯例约束的成套称谓**，已改为惯例优先并自动提取姓（现输出 `Matron Zou`）。
  2. 同一人物的括号异写（`赵朴（仪王）`）被重复收录 → 已加别名检测。
  3. `内侍` 等通称因命中 RAG 角色切片名被归入"一 角色人名" → 分类轻微误差，**未自动修**（改名需用户确认），留待人工裁定。

## 四、临时方案 / 已知妥协

| # | 临时处理 | 何时替换 |
|---|---|---|
| T1 | `_记忆索引.md` 未建，当前用 `项目目录索引.md` + 8 份日志定位状态 | 任务 4 |
| T2 | 门禁 G1/G8/G9 暂为人工项，无脚本 | 任务 3 后评估是否脚本化 |
| T3 | 术语候选抽取仅基于各章 `第X章术语与人设.md`（不做中文分词，无第三方库依赖） | 若召回不足，再考虑引入 jieba |
| T4 | `glossary.md` v0.9 中 304 条 `[建议]` 未经人工裁定，默认不得直接用于新章初译 | 用户抽查确认后批量转 `锁定` |
| T5 | 分类为规则式（关键词/后缀匹配），存在轻微误差（如"内侍"归为角色人名） | 人工裁定时顺手调整分类 |
| T6 | `project_status.py` 槽位匹配为"文件名包含"启发式，20 种命名变体下统计会漂移 | 任务 9（命名归一）完成后改为精确前缀规则 |
| T7 | 体量基准按"单章中位 2,201 英文词"线性外推全书，未逐章精算 | 若某批章节体量显著偏离（±50%），按批重算投影 |

## 五、执行记录（按时间倒序）

### 2026-09-06 15:28:58 · 规模化诊断（无翻译产出）

**性质**：全量实测 + 结构/工作流优化建议，**未改动任何译文或术语**。
**产出**：`项目文档/规模化诊断与优化建议_20260906_152858.md`、`项目日志_09_20260906_规模化诊断与结构优化.md`。

**实测数据（首次量化）**

| 指标 | 数值 | 方法 |
|---|---|---|
| 全本章数 | 351 | 总底稿正则匹配章节标题（第1章~第351章） |
| 全本字数 | 1,323,298 汉字 | 字符统计 |
| 单章中位 | 2,637 汉字 → 2,201 英文词 | 71 章原文 + 84 章对照 |
| 已产英文词 | 182,570 | 84 个对照版合计 |
| 章节 / 字数完成度 | 25.6% / 23.9% | 90/351；18.3万/76.3万 |
| **剩余** | **261 章 ≈ 58 万英文词** | 第91~351章，目录未建 |

**关键结论**

1. **Q7 澄清**：351 章与 90 章不矛盾——90 是已建目录数。第91~351章连目录都未建立，任务 6 原范围（26 章）严重低估剩余工作。
2. **停滞确认**：重跑 `project_status.py`，全部统计项与 09-02 基线一致 → 4 天零进展。
3. **真瓶颈识别**：不是翻译能力，是「可验证性缺失」+「冷启动成本过高」。26 章缺口 / 12 章译文缺失 / 78 vs 84 不同步，一个 `sync_bilingual.py` 即可收敛。
4. **唯一真阻塞**：304 条术语全裁 = 纯人工瓶颈，AI 无权代裁 → 改分批解锁（D9）。
5. **唯一"越做越快"的杠杆**：TM 句级记忆。已产 18.2 万词却零 segments。

**新增风险 R1–R4**：git 未落盘（57 项变更 / 最后提交 08-02 / `.git.corrupt` 残留）、三份副本（主 69.4MB + `副本` 44.9MB + `_recovery_clone` 13.3MB）、命名 20 种变体、TM 为零。

**新增任务 7–10**：git 基线（P0-0）/ 切分第91-351章原文 / 命名归一 + 副本收敛 / TM 抽取。

**可复用经验（写入工程备注）**

- **中文路径坑绕过**：PowerShell 直传中文绝对路径必 GBK 乱码（`The string is missing the terminator`）。可行法：命令行全程 ASCII，用 `glob.glob('d:/MyData/projects/*/*/*/*/tools/xxx.py')` 在 Python 内推导路径；需匹配中文时用 Unicode 转义（`\u7b2c`=第、`\u7ae0`=章）。
- **体量均匀性**：单章中位与均值几乎重合（2,201 vs 2,173），方差小 → 工作量可按"章数 × 2,200 词"线性外推，无需逐章估算。

### 2026-09-06 19:xx · 任务 3 起步 + 跨工作区记忆检索（接续）

**任务 3 首个脚本：`tools/sync_bilingual.py` v1（✅ 已跑通）**

- **对照版体制实测**：84 章共 3 种——A-sep（77 章，`---` 分隔）+ B-plain（4 章：6/7/44/47）+ C-marked（3 章：8/9/10，`**原文**/**译文**` 显式标记，且存在半角冒号 `**译文**:` 变体）。
- **解析器要点**：① 标题行过滤（`第X章 …` 无 `#` 旧体制）② **续行合并**（`>` 引用多行诗，第37章实证，成对后紧随的诗行须追加到上一对英文而非开新块）③ 中→英配对，英文在前为"顺序倒置"记异常。
- **门禁落点**：G1（原文 MD5 `--init-md5` 基线）· G2（段落 1:1）· G12（三件套清单）。
- **全量首扫修正后**：已同步 29 / 缺纯英版 7 / 不同步 45 / 无对照版 6。
  - **教训**：初版把纯英版 `---` 分隔行计为段落 → 误报 68 章不同步；实际多数章节段数本就 1:1（原文段=对照块=纯英段）。**先修解析 bug 再下结论**，勿把误报当欠账。
- **写入策略**：`--apply` 只补缺失的译文版；覆盖已存在但不同步的需 `--force`（无 git 回滚点前保持保守）。
- **报告**：`项目文档/双语同步校验报告_20260906_190948.md`（dry-run 全量明细）。
  - ⚠️ `--report` 曾因 PowerShell 中文参数 GBK 乱码误建乱码目录（`椤圭洰鏂囨。`，已清理），已改为 `--report`（不带值）自动命名到 `项目文档/`。

**跨工作区记忆检索发现（01-活跃项目 / 中英剧本MTPE / 07_外贸业务 .codebuddy/memory）**

- **用户红线（2026-09-03，多处重复记载）**：新建工程性文件只给框架，`{{占位符}}` 禁代填 → **直接影响任务 4/5 的写法**（`_记忆索引.md`/`README.md`/`project_rules.md`/`novel_rules.md`/三 agent 全须骨架化）。
- **术语回溯系统性缺陷（D13 候选）**：任务 2b 只扫各章术语文件、未扫 8 份项目日志 + Tab 表历史裁定 → 已裁定项被降级。**实证：夏果 三重冲突**（Tab=Xia Guo / 日志05 L587=Xia Gu / glossary v0.9=Xiaguo）。补救动作已写入 TODO §二·〇。
- 新增跨项目可复用铁律 9 条（占位符框架/系统时间/外部审校四象限/五类致命缺陷/蒸馏通用化/剥离 LOCAL 层/git 大量 D 确认/编辑 ≤3 次/双 skill 前移），详见项目长期记忆。
- **上层记忆路径已更正**：`01-活跃项目/.codebuddy/memory/MEMORY.md` 中本项目路径由 `02_网文创作/` 更正为 `01_翻译业务/`。

### 2026-09-02

- 建立本文件与 `TODO.md`，确立 7 项任务与 Q1–Q5 未决问题。
- 方案自审校完成：修正 4 处事实错误（A1–A4）、软化 2 处过度建议（B1–B2）、保留 3 项方向性质疑（C1–C3），详见方案文末《自审校记录》。
- **发现并校正重大不一致**：磁盘实测 **90 章**（第1~90章无缺号），而 `项目目录索引.md` 记录 35 章。已在索引顶部加滞后警示，进度基准切换为 `项目文档/项目进度基线.md`（D7）。
- 新增 `tools/project_status.py`（进度基线扫描器）与 `tools/build_glossary_candidates.py`（术语冲突扫描器），均遵循 MTPE 工具规范（dry-run 默认 / 只读 / 仅标准库 / UTF-8 / 路径自推导）。
- 完成任务 0（知识库路径修复）、任务 1（humanize 接线）、任务 2a（术语候选与冲突清单）、任务 2b（`glossary.md` v0.9）。
- 新增工具：`tools/project_status.py`（进度基线）、`tools/build_glossary_candidates.py`（术语冲突扫描 + `--emit-glossary` 生成主表）。
- 关键经验（可复用）：**"按出现频次推荐译法"在有成套惯例的称谓上会失效**——`邹嬷嬷` 频次最高译法是 `Nurse Zou`，但正确为 `Matron Zou`。结论：术语自动化中，**惯例规则优先级必须高于频次统计**。
- 新增未决问题 Q6（19 章无原文）、Q7（90 章 vs 索引称 351 章）、Q8（第1-9章仅术语无译稿）。
- 方案自审校完成：修正 4 处事实错误（A1–A4）、软化 2 处过度建议（B1–B2）、保留 3 项方向性质疑（C1–C3），详见方案文末《自审校记录》。
- 新增发现：`.agent/归档/knowledge/terminology/` 混入非术语切片（`3.3 笨拙的温柔.md`、`三、本章翻译注意事项.md`、`4. 剧情进展.md`），重建时一并清理。
